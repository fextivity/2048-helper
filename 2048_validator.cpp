#include "testlib.h"
#include <bits/stdc++.h>
using namespace std;

signed main(int argc, char* argv[]){
    registerValidation(argc, argv);
    int seed = inf.readInt(1, 1000000000, "seed");
    inf.readEoln();
    inf.readEof();
}